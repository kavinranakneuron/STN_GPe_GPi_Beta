# Project memory

This is the JNE-110355 rebuild. Before doing anything, read:

- `AGENTS.md` — operational guide with phase plan, decision points, and guardrails
- `docs/rebuild_scope.md` — why-document with full justification of design decisions

Working branch is `rebuild`. The `legacy/` directory contains the original
submitted codebase for reference only — do not modify it.

Commit at the end of each numbered step within a phase. At phase boundaries,
run all relevant tests, push to origin, and pause for human review before
proceeding.
