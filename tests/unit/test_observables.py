"""Phase 1 placeholder: observables module is built in Phase 2.

This file exists so the unit-test layout is complete and obvious to future
agents. The real observables tests (firing rate, CV, beta fraction) live
here once bgnet/observables.py is implemented.
"""
import pytest

pytest.skip("observables module belongs to Phase 2", allow_module_level=True)
